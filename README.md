# dotfiles

## vim

How to link up `vim`:

```bash
ln -s "$(pwd)/.vim" ~/
```

To remove:

```bash
rm ~/.vim
```

## zsh

How to link up `zsh`:

```bash
rm ~/.zshrc

ln -s "$(pwd)/zsh/.zshrc" ~/.zshrc
```

To remove:

```bash
rm ~/.zshrc
```
